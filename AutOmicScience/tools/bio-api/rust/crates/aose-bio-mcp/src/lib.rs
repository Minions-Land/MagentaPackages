//! aose-bio-mcp library: bio-database typed tools.
//!
//! Vendored from AutOmicScience `aose-tools::bio_typed` (a single self-contained
//! file). Re-exposed here as its own crate so the MCP server binary can register
//! the tool set without pulling the heavy aose-tools graph.
//!
//! Origin: github.com/Minions-Land/AutOmicScience, crate `aose-tools` (bio_typed.rs).

pub mod bio_typed;
